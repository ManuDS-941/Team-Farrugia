{% extends "base.html.twig" %}

{% block title %} Ajouter un Champion {% endblock  %}

{% block body %}

<h1 class="text-center mt-4">Ajout d'un Champion</h1>

{{form_start(formChampion, {'attr': { 'class': 'col-md-4 mx-auto'}} )}}

<div class="form-group">
    {{ form_row(formChampion.nom, {'label': "Nom :", 'attr': {'class': 'form-control'}}) }}
</div>

<div class="form-group">
    {{ form_row(formChampion.image, {'type': "file", 'attr': {'class': 'form-control'}}) }}
</div>

<div class="form-group">
    {{ form_row(formChampion.palmares, {'label': "Palmarès", 'attr': {'class': 'form-control'}}) }}
</div>

<div class="text-center">
    <a href="{{ path('admin_medias') }}" class="col-5 mr-4 btn btn-dark">Retour</a>
    <button type="submit" class="col-5 btn btn-success mt-4 mb-4">Ajouter</button>
</div>

{{form_end(formChampion)}}

{% endblock %}