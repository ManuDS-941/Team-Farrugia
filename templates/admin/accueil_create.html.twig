{% extends "base.html.twig" %}

{% block body %}

<h1 class="text-center mt-4">Ajout d'un Article à l'Accueil</h1>

{{ form_start(formAccueil, {'attr': { 'class': 'col-md-4 mx-auto'}} )}}

<div class="form-group">
    {{ form_row(formAccueil.titre, {'label': "Titre", 'attr': {'class': 'form-control'}}) }}
</div>

<div class="form-group">
    {{ form_row(formAccueil.image, {'type': "file", 'attr': {'class': 'form-control'}}) }}
</div>

<div class="form-group">
    {{ form_row(formAccueil.description, {'label': "Description", 'attr': {'class': 'form-control'}}) }}
</div>

<div class="text-center">
    <a href="{{ path('admin_accueil') }}" class="col-5 mr-4 btn btn-danger">Retour</a>
    <button type="submit" class="col-5 btn btn-dark mt-4 mb-4">Ajouter</button>
</div>

{{ form_end(formAccueil)}}
    
{% endblock %}